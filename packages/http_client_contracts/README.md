# http_client_contracts

Core HTTP abstractions designed for dependency-inverted networking in Dart and Flutter.

## Includes

- `HttpClient` interface
- Immutable `HttpRequest`, `HttpResponse`, and `HttpStreamResponse`
- Typed request bodies
- Typed exceptions and cancellation token

Use this package in feature/data layers and inject adapters from separate packages.
