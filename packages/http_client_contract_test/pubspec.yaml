name: http_client_contract_test
description: Shared HTTP client contract tests for adapter packages.
version: 0.1.0
homepage: https://github.com/hashem/http_client
repository: https://github.com/hashem/http_client

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  http_client_contracts: ^0.1.0
  test: ^1.25.8

dev_dependencies:
  lint: ^2.3.0
