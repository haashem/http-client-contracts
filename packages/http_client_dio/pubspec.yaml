name: http_client_dio
description: Dio adapter for the http_client_contracts contract.
version: 0.1.0
homepage: https://github.com/hashem/http_client
repository: https://github.com/hashem/http_client

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  dio: ^5.9.0
  http_client_contracts: ^0.1.0

dev_dependencies:
  http_client_contract_test: ^0.1.0
  lint: ^2.3.0
  test: ^1.25.8
